For document cleaning code
Takes in a string with extra spaces and excess characters and using a regular expression to replace all spaces with one space each and replaces all non words and non
spaces with an empty string. Runtime is O(n) where n is the number of characters.